# 📖 博客日常操作指南

## 📝 添加新文章

### 步骤 1：创建文章文件
```powershell
cd C:\Users\ZZY\my-mizuki-blog\src\content\posts
```

### 步骤 2：创建 markdown 文件
文件名建议用英文或拼音（避免特殊字符），例如：
- `my-new-post.md`
- `wo-de-xin-wen-zhang.md`

### 步骤 3：填写文章内容
```markdown
---
title: 文章标题
published: 2026-04-12  # 格式：YYYY-MM-DD
description: 文章简介（显示在文章列表中）
tags: [标签 1, 标签 2]   # 多个标签用逗号分隔
category: 分类名       # 如：技术、生活、随笔
---

# 文章标题（可选，frontmatter 中已有 title）

这里是文章内容...

## 小标题

支持 Markdown 格式：
- **加粗**
- *斜体*
- [链接](https://example.com)
- 代码块
- 图片 ![描述](/path/to/image.png)
```

### 步骤 4：提交并部署
```powershell
cd C:\Users\ZZY\my-mizuki-blog
git add .
git commit -m "添加新文章：文章标题"
git push origin master
```

---

## ✏️ 修改已有文章

### 步骤 1：找到文章文件
```powershell
cd C:\Users\ZZY\my-mizuki-blog\src\content\posts
ls  # 查看文章列表
```

### 步骤 2：编辑文章
```powershell
# 用 VS Code 打开
code 文章文件名.md

# 或用记事本
notepad 文章文件名.md
```

### 步骤 3：保存并提交
```powershell
cd C:\Users\ZZY\my-mizuki-blog
git add .
git commit -m "更新文章：文章标题"
git push origin master
```

---

## ⚙️ 修改网站配置

### 修改网站标题、副标题、URL 等
```powershell
# 打开配置文件
code C:\Users\ZZY\my-mizuki-blog\src\config.ts
```

修改以下字段：
```typescript
export const SITE = {
  title: "你的网站标题",
  subtitle: "你的副标题",
  lang: "zh_CN",
  site: "https://wangtanzhi6.github.io/",
  // ...
}
```

### 修改音乐播放器配置
```typescript
// 在 config.ts 中找到 MUSIC 配置
export const MUSIC = {
  mode: "meting",  // "meting" 或 "local"
  server: "https://music.163.com/",  // 网易云音乐服务器
  type: "playlist",  // "playlist" 或 "song"
  id: "你的歌单 ID",  // 替换成你的歌单 ID
  // ...
}
```

### 修改主题颜色
```typescript
// 在 config.ts 中找到 THEME 配置
export const THEME = {
  color: {
    hue: 240,  // 色相值 (0-360)
    // ...
  }
}
```

### 保存并提交
```powershell
git add .
git commit -m "更新网站配置"
git push origin master
```

---

## 🗑️ 删除不需要的功能/内容

### 删除相册功能

相册功能通常在以下位置：

#### 1. 删除相册文章
```powershell
# 查看是否有相册相关文章
cd C:\Users\ZZY\my-mizuki-blog\src\content\posts
ls | Select-String "相册|gallery|photo"

# 删除相册文章文件
Remove-Item "相册文章文件名.md"
```

#### 2. 删除导航栏相册链接
```powershell
# 打开导航配置文件
code C:\Users\ZZY\my-mizuki-blog\src\config.ts

# 找到 header 或 nav 配置，删除相册相关条目
# 例如：
{
  text: "相册",
  link: "/gallery",
  icon: "📷"
}
// 删除或注释掉这一项
```

#### 3. 删除相册组件（如果存在）
```powershell
# 查找相册组件
cd C:\Users\ZZY\my-mizuki-blog
Get-ChildItem -Recurse -Filter "*gallery*" -Include *.astro,*.vue,*.tsx

# 删除找到的相册组件文件
```

#### 4. 提交更改
```powershell
git add .
git commit -m "移除相册功能"
git push origin master
```

---

### 删除其他不需要的功能

#### 删除文章
```powershell
cd C:\Users\ZZY\my-mizuki-blog\src\content\posts
Remove-Item "不需要的文章.md"
```

#### 删除侧边栏/导航栏项目
```powershell
# 编辑 config.ts
code src\config.ts

# 找到对应的配置项（如 socialLinks, header, sidebar 等）
# 删除不需要的条目
```

#### 删除页面组件
```powershell
# 查找页面组件
cd C:\Users\ZZY\my-mizuki-blog\src\pages
ls

# 删除不需要的页面文件
Remove-Item "页面名称.astro"
```

---

## 🎨 添加新功能

### 添加新页面
```powershell
# 创建新页面
cd C:\Users\ZZY\my-mizuki-blog\src\pages

# 创建页面文件（如 about.astro）
New-Item "about.astro"
```

页面内容示例：
```astro
---
// 页面标题
const title = "关于我";
---

<html>
  <head>
    <title>{title}</title>
  </head>
  <body>
    <h1>{title}</h1>
    <p>这里是页面内容...</p>
  </body>
</html>
```

### 在导航栏添加新页面链接
```powershell
code src\config.ts
```

在 `header` 或 `nav` 配置中添加：
```typescript
{
  text: "关于我",
  link: "/about",
  icon: "👤"
}
```

---

## 🔍 常用命令速查

```powershell
# 进入博客目录
cd C:\Users\ZZY\my-mizuki-blog

# 查看文章列表
ls src\content\posts

# 本地预览（开发服务器）
pnpm dev
# 访问 http://localhost:4321

# 构建项目（检查是否有错误）
pnpm build

# 提交所有更改
git add .
git commit -m "提交信息"

# 推送到 GitHub（触发自动部署）
git push origin master

# 查看 Git 状态
git status

# 查看最近的提交记录
git log --oneline -5
```

---

## 📋 操作清单

### 添加文章后检查
- [ ] 文章 frontmatter 格式正确
- [ ] 本地预览正常（`pnpm dev`）
- [ ] 提交到 Git（`git add . && git commit -m "..."`）
- [ ] 推送到 GitHub（`git push origin master`）
- [ ] 访问 GitHub Actions 确认部署成功
- [ ] 访问博客确认文章显示

### 修改配置后检查
- [ ] 配置文件语法正确（无错别字、引号匹配）
- [ ] 本地预览查看效果（`pnpm dev`）
- [ ] 提交并推送
- [ ] 确认部署成功

---

## 🆘 常见问题

### 1. 文章不显示
- 检查 frontmatter 格式是否正确
- 检查 `published` 日期是否有效
- 查看构建日志是否有错误

### 2. 构建失败
```powershell
# 清理并重新安装
cd C:\Users\ZZY\my-mizuki-blog
Remove-Item -Recurse -Force node_modules
pnpm install
pnpm build
```

### 3. 中文乱码
- 确保文件用 UTF-8 编码保存
- VS Code 右下角点击编码 → 选择 "通过编码保存" → UTF-8

### 4. 部署失败
- 查看 GitHub Actions 日志：https://github.com/Wangtanzhi6/wangtanzhi6.github.io/actions
- 检查是否有构建错误
- 确保 Node.js 版本 >= 22

---

## 📞 需要帮助？

遇到问题可以：
1. 查看 GitHub Actions 日志找错误
2. 查看 Mizuki 官方文档：https://docs.mizuki.mysqil.com/
3. 查看项目 README：`C:\Users\ZZY\my-mizuki-blog\README.md`

---

*最后更新：2026-04-12*
